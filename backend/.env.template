# Copy this to .env and edit values as needed
MONGO_URI=mongodb://localhost:27017/startup_ai
STORAGE_PATH=./uploads
USE_CLOUD=false
GCLOUD_BUCKET=
LLM_PROVIDER=gemini
REDIS_URL=redis://localhost:6379/0
GEMINI_API_KEY=
USE_VISION=false

# If using Google Cloud (Vision or Storage), ensure credentials are available.
# Either set GOOGLE_APPLICATION_CREDENTIALS to a local service account JSON path
# or run in an environment with default credentials.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service_account.json


# MONGO_URI=mongodb://localhost:27017/startup_ai
# STORAGE_PATH=./uploads
# USE_CLOUD=false
# GCLOUD_BUCKET=
# LLM_PROVIDER=custom   # "custom", "openai", "gemini" etc.
# REDIS_URL=redis://redis:6379/0
# GEMINI_API_KEY=your_key_here
# USE_VISION=false
